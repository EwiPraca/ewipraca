<script type="text/javascript">
    $("#open-change-password-modal").click(function () {
        showLoader();
        var options = { keyboard: true };
        $.ajax({
            type: "GET",
            url: "@Url.Action("ChangePasswordView", "Manage")",
            contentType: "application/json",
            datatype: "json",
            success: function (data) {
                $('#change-password-modal-content').html(data);
                $('#change-password-modal').modal(options);
                $('#change-password-modal').modal('show');
                hideLoader();
            },
            error: function () {
                hideLoader();
                alert("Dynamic content load failed.");
            }
        });
    });

    $("#closbtn").click(function () {
        $('#change-password-modal').modal('hide');
    });
</script>

<div id='change-password-modal' class='modal'>
    <div class="modal-dialog popup-modal">
        <div class="modal-content">
            <div id='change-password-modal-content'></div>
        </div>
    </div>
</div>